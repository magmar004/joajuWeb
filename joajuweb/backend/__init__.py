"""
Backend unificado (una sola app Django).

    models/     datos por tema (usuarios, actividades)
    views/      endpoints HTTP por tema
    forms/      formularios por tema
    servicios.py  guardar / editar / cancelar / inscribir
    notificaciones.py  aviso a inscriptos
    urls.py     /voluntario y /coordinador
    tests/      pruebas por tema
"""
